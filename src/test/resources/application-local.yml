spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: group-1
      auto-offset-reset: earliest
    admin:
      properties:
        bootstrap:
          servers: localhost:9092